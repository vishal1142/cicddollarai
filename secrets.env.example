# Copy to secrets.env and fill in. Do not commit secrets.env.
# Used by: python scripts/jenkins_bootstrap.py

JENKINS_URL=http://localhost:8080
JENKINS_USER=admin
# Use your Jenkins password OR an API token (recommended: User -> Configure -> API Token)
JENKINS_PASSWORD=changeme

GITHUB_USERNAME=vishal1142
# GitHub PAT: fine-grained or classic; needs repo scope for private repos / contents read
GITHUB_PAT=

# Optional overrides
GITHUB_REPO_URL=https://github.com/vishal1142/java.git
JENKINS_JOB_NAME=java-ci
JENKINSFILE_PATH=Jenkinsfile
